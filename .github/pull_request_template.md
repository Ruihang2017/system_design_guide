## What this changes

Briefly describe the content added or fixed, and which page(s) it touches.

## Checklist

- [ ] Page has frontmatter (`title` + one-line `description`); body headings start at `##`.
- [ ] New pages are added to the `sidebar` in `astro.config.mjs`.
- [ ] Any technical/real-world claims are cited to a real source.
- [ ] `npm run build` passes (exit 0, no broken-link warnings).
- [ ] Voice matches the site (crux-first, tradeoff-focused).
